{
  "name": "interview-agent-electron",
  "private": true,
  "version": "0.1.0",
  "description": "Interview-Agent Electron desktop shell (dev-only). Loads Vite dev at :5173; does not spawn the Go backend.",
  "main": "out/main/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && electron .",
    "postinstall": "node scripts/patch-electron-bundle-id.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "electron": "^40.1.0",
    "typescript": "^5.9.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron"
    ]
  }
}
